{
  "name": "dryline",
  "version": "0.0.1",
  "private": true,
  "description": "Investigate Texas water at any address. Environmental intelligence for a thirsty state.",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev:mcp": "pnpm --filter @dryline/mcp dev",
    "dev:web": "pnpm --filter @dryline/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
